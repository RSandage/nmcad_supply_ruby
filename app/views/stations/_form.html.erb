<div style="margin: 0 25%: width: 50%">
  <%= render 'shared/errors', obj: @station %> <%= form_with(model: @station) do
  |form| %> <% end %>
  <div class="mb=3"></div>
</div>

<div style="margin: 0 25%; width: 50%">
  <%= form_with(model: @station) do |form| %>
  <div class="mb-3">
    <%= form.label :name, "Name:", class:"form-label" %>
    <%=form.text_field :name, class:"form-control" %>
  </div>
  <%= form.submit(@station.new_record? ? 'Add Station' : 'Edit Station', class:"btn btn-primary") %> <% end %>
</div>